Usage: upbrr api-token create [options]

Options:

Config:
  -config, --config string
      Path to config file

Other:
  -name, --name string
      Operator-facing token name
  -owner, --owner string
      Workflow owner isolation key
  -scopes, --scopes string
      Comma-separated API scopes
